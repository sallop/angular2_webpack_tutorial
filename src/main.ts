import 'zone.js'
import 'reflect-metadata'

import {bootstrap} from 'angular2/platform/browser'
import {AppComponent} from './app.component.ts';
//import {AppComponent} from './app.component'

document.write("it's a dildo. Of course it's company policy never to, imply ownership in the event of a dildo... always use the indefinite article a dildo, never your dildo.");
 
//document.write("Worker bees can leave. Even drones can fly away. The Queen is their slave.");
//bootstrap(AppComponent);
document.addEventListener("DOMContentLoaded", function(event){
  bootstrap(AppComponent);
});
